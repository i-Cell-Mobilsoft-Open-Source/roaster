coffee:
  redis:
    test:
      # We set the host and port programmatically because they are dynamic during test execution
      # host: 172.17.0.4
      # port: 6379
      # password: nincs
      database: 0
      pool:
        maxtotal: 6 #default: 64
        maxidle: 6 #default: 64
      timeout: 5000 #default: 5000